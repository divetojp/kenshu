# 因子分析

**観測できない潜在的な共通因子** を発見するための統計モデルです。PCA が次元削減の道具であるのに対し、因子分析は「なぜ変数間に相関があるのか」という構造の解釈が目的です。アンケート分析・心理尺度・社会科学データでよく使われます。

---

## モデル式

$$
x_i = \lambda_{i1} F_1 + \lambda_{i2} F_2 + \cdots + \varepsilon_i
$$

- $F_j$：潜在因子（直接観測できない）
- $\lambda_{ij}$：因子負荷量（変数 $i$ と因子 $j$ の関係の強さ）
- $\varepsilon_i$：独自因子（その変数固有の部分）

---

## 実装

```python
from sklearn.decomposition import FactorAnalysis
from sklearn.preprocessing import StandardScaler
from sklearn.datasets import load_iris
import pandas as pd

data = load_iris()
X_scaled = StandardScaler().fit_transform(data.data)

fa = FactorAnalysis(n_components=2, random_state=42)
fa.fit(X_scaled)

# 因子負荷量行列
loadings_fa = pd.DataFrame(
    fa.components_.T,
    index=data.feature_names,
    columns=["Factor1", "Factor2"]
)
print(loadings_fa.round(3))
```

---

## PCA vs 因子分析

| 観点 | PCA | 因子分析 |
|------|-----|---------|
| 目的 | 次元削減・圧縮 | 潜在構造の発見・解釈 |
| モデル | 統計モデルなし（行列演算） | 確率モデルあり |
| 誤差の扱い | 全分散を説明しようとする | 共通分散のみ説明する |
| 因子の数 | 寄与率で決める | 適合度・解釈性で決める |
| 典型的な使用場面 | ML の前処理・可視化 | 心理尺度・アンケート分析 |

---

## 確認問題

1. 因子分析と PCA の目的の違いを説明してください。
2. 因子負荷量とは何を表すか説明してください。
3. 独自因子 $\varepsilon_i$ が意味するものは何ですか？

---

## 関連ページ

- [主成分分析（PCA）](主成分分析.md) — 次元削減の代表的手法
- [線形代数](線形代数.md) — 行列分解の数学的背景
- [確率・統計基礎](確率・統計基礎.md) — 分布・共分散の基礎
